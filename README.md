# entropyof

entropyof is a simple Linux command-line utility to rapidly compute the Shannon entropy of a file.

## Theory

## Installation

```{shell}
$ git clone 
$ cd ./entropyof 
$ make
$ sudo make install
```
